# Project workflow preferences

- Open the PR in the browser when a pull request is created.
- Open the PR in the browser when a pull request is manually edited or updated.
- Do not open the PR in the browser as part of merge-only actions.
- When preparing a release, also set up metadata files for release notes (localized metadata / patch note files) before release submission.
